# EventSphere Frontend (React + TS + Vite)

## Scripts
- npm run dev
- npm run build
- npm run preview

## Env
- VITE_BACKEND_URL (default http://localhost:4000)
- VITE_MAP_TILE_URL (OpenStreetMap tiles)

## Notes
- Uses Tailwind for minimal styling
- Socket.IO client connects to backend URL
